[build]
  command = "cd Gym-Saga-Design && pnpm install --no-frozen-lockfile && pnpm --filter @workspace/profeandres run build:web"
  publish = "Gym-Saga-Design/artifacts/profeandres/dist"

[build.environment]
  NODE_VERSION = "24"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
